[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
toml = "^0.8"
log = "0.4"

[lib]
name = "shared"
path = "src/lib.rs"  # Явно указываем путь к библиотеке